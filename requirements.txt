# ─────────────────────────────────────────────
# Smart Agriculture Assistant App
# Requirements — install with:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core framework
kivy>=2.2.1
kivymd>=1.1.1          # optional: for Material Design widgets

# ML/AI (for real plant disease prediction)
tensorflow>=2.12.0     # or tensorflow-lite for mobile
numpy>=1.23.0
Pillow>=9.0.0

# Text-to-Speech (offline, works without internet)
pyttsx3>=2.90

# Utilities
requests>=2.28.0       # alternative to urllib for weather API

# ─────────────────────────────────────────────
# MINIMAL INSTALL (if TF is too heavy):
#   pip install kivy pyttsx3
# Then the app runs in DEMO MODE (no real model)
# ─────────────────────────────────────────────
